import styled from 'styled-components';

import logo from '../../assets/img/logoquental.png'

const LogoQD = styled.img.attrs({ src: logo, alt: 'Logo QuentalDev' })`
    height: 40rem;
`;

export default LogoQD;